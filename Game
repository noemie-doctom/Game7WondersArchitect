import java.util.Collections;
import java.util.ArrayList;
import java.util.List;

public class Main {
    public static void main(String[] args) {
        System.out.println("Hello");

        Pioche();

    }

    public void Pioche() {

        List<Card> myList = new ArrayList<>();

        myList.addAll(Deck);
 //Deck est la liste qui contient toutes les cartes

        Collections.shuffle(myList);
        return;
// Choisit un élément ramdom de ta liste

 // Trouver un moyen de retourner l'objet choisit et de le supprimer de la pioche

        for (int i = 0; i < myList.size(); i++) {
            if (Object.equals(Object, myList.get(i))) {
                list.remove(i);
            }
        }

        // Trouver un moyen de retourner l'objet choisit et de le supprimer de la pioche
    }


    public void RetournerCarte() {
        int g;
        for (g = 0; g < myList.size(); g++) {
            System.out.println("La carte se trouvant au dessus est : " + (Object, myList.get(g)));
        }
    }
// Permet d'afficher la dernière carte

    }

}
